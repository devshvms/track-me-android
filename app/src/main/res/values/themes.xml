<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AppCompatActivity is required for backward-compatible per-app locales in Compose. -->
    <style name="Theme.TrackMe" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <!-- App Actions' required Activity fulfillment with no window, transition, or visible task. -->
    <style name="Theme.TrackMe.VoiceAction" parent="android:style/Theme.NoDisplay" />
</resources>
