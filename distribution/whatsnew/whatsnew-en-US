Maps are working again in the signed release. This update restores map tiles, ride routes, current-location display, and map snapshots. It also prevents future release builds from shipping without a configured Maps API key.
