TrackMe 1.8.0

• Warning text is readable again. The amber used for warnings failed the accessibility contrast minimum on light backgrounds.

• Cleaner surfaces. Cards, sheets and the navigation bar now sit at genuinely different levels instead of blending into the background.

• Dividers and borders no longer look identical, so lists and settings are easier to scan.

Tracking, group rides, sync and export are unchanged.
